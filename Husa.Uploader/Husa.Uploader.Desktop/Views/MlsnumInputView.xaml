<Window x:Class="Husa.Uploader.Desktop.Views.MlsnumInputView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:mam="http://metro.mahapps.com/winfx/xaml/controls"
        xmlns:local="clr-namespace:Husa.Uploader.Desktop.Views"
        mc:Ignorable="d"
        Title="MLS Number"
        MinHeight="100"
        MinWidth="300"
        Width="300"
        WindowStyle="ToolWindow"
        ShowInTaskbar="False"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <StackPanel Margin="10">
        <TextBlock>We need the listing's MLS Number to continue</TextBlock>
        <TextBox Name="MlsNum" Text="{Binding MlsNum, UpdateSourceTrigger=PropertyChanged}"></TextBox>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Name="Continue" Padding="7" Margin="0,5,0,0" Command="{Binding ContinueCommand}">Continue</Button>
            <Button Name="Cancel" Padding="7" Margin="5,5,0,0" Command="{Binding CancelCommand}">Cancel</Button>
        </StackPanel>
    </StackPanel>
</Window>
